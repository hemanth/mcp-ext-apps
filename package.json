{
  "name": "mcp-ui-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "npm run build && npm run server",
    "build": "vite build && cp -r src/host/* dist/",
    "server": "npx tsx src/server.ts",
    "host": "npx http-server dist -p 8080 -c-1 --cors"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.52.0",
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.2",
    "@types/node": "^22.15.29",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vite-plugin-singlefile": "^2.0.3"
  }
}
